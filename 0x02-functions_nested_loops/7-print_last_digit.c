#include "main.h"
/**
 * print_last_digit - Print the last digit of a number
 * @n: the prefered argument
 *
 * Return: The value of the last digit
 */
int print_last_digit(int n)
{
	return (n % 10);
}
