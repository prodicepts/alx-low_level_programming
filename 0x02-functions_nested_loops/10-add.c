#include "main.h"
/**
 * add - Adds two integers and return the results
 * @a: first argument
 * @b: second argument
 *
 * Return: Sum of the two integers
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
